#!/usr/bin/env bash
echo "Initial build, just to be safe."
make
echo "Done! Build watch started."
inotifywait -mq -e create -e modify -e delete -e move ./source |
    while read dir action file; do
        echo "The file '$file' appeared in directory '$dir' via '$action', running make..."
        make
    done
